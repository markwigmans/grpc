spring:
  application:
    name: json-server

server:
  port: 8090

eureka:
  instance:
    nonSecurePort: ${server.port}
  client:
    serviceUrl:
      defaultZone: 'http://${eureka.host:localhost}:${eureka.port:8761}/eureka/'